# .gitignore - Files and directories to exclude from Git

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual Environment
venv/
env/
ENV/
.venv/
.ENV/

# Database files (don't commit real patient data!)
*.db
*.sqlite
*.sqlite3
patients.db
people.db
people_backup_*.db

# PostgreSQL backups and exports
backup_*.sql
backup_*.db
export_*/
*.dump

# Migration and utility scripts (temporary)
migrate_*.py
_service_schema_patch.py
fix_database.py
check_schema.py
restart_admin.py
setup.py
simple_setup.py
migrations_archive/

# Migration and temporary files
pre_migration_report.json
post_migration_report.json
migration_*.log

# Uploads directory (patient files - sensitive data)
uploads/

# Audit and compliance reports (may contain PHI)
hipaa_audit.log
hipaa_compliance_report_*.json
hipaa_audit_analyzer.py

# Debug files
debug.html

# IDE and Editor files
.vscode/
.idea/
*.swp
*.swo
*~
.sublime-workspace
.sublime-project

# Operating System files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs and temporary files
*.log
*.tmp
*.temp
.cache/

# Environment variables (NEVER COMMIT THESE - CONTAIN CREDENTIALS!)
.env
.env.local
.env.development
.env.production
.env.development.local
.env.test.local
.env.production.local

# AWS and deployment credentials (NEVER COMMIT THESE!)
aws-db-credentials.txt
.aws/
*.pem
*.key

# Elastic Beanstalk Files (generated during deployment)
.elasticbeanstalk/*
!.elasticbeanstalk/*.cfg.yml
!.elasticbeanstalk/*.global.yml

# Coverage reports (if you add testing)
htmlcov/
.coverage
.coverage.*
coverage.xml
*.cover
.hypothesis/
.pytest_cache/

# Jupyter Notebook (if you add data analysis)
.ipynb_checkpoints

# pyenv
.python-version

# Security and secrets (always exclude!)
secrets.txt
config.ini
*.pem
*.key

# Local files directory (database backups, migrations, logs, etc.)
local_files/

# Environment files with sensitive data (already covered above)
# .env.development (moved to main ignore section)
# .env.local (moved to main ignore section) 
# .env.staging (if you create one)
# .env.test (if you create one)